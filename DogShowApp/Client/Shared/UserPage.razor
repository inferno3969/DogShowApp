@using DogShowApp.Client.Pages
@using DogShowApp.Shared.Data
<Bar Breakpoint="Breakpoint.Desktop"
     Background="Background.Light"
     ThemeContrast="ThemeContrast.Light">
    <BarBrand>
        Dog Show
    </BarBrand>
    <BarToggler />
    <BarMenu>
        
        <BarStart>
            <BarItem>
                <BarLink To="/">Home</BarLink>
            </BarItem>
            <BarItem>
                <BarLink To="/fetchevents">Events</BarLink>
            </BarItem>
            <BarItem>
                <BarLink To="/tickets">Tickets</BarLink>
            </BarItem>
            <BarItem>
                <BarDropdown>
                    <BarDropdownToggle>Bios (Maybe)</BarDropdownToggle>
                    <BarDropdownMenu>
                        <BarDropdownItem To="/fetchbio">Dog Biographies</BarDropdownItem>
                        <BarDropdownDivider />
                        <BarDropdownItem To="/fetchbiotrainer">Trainer Biographies</BarDropdownItem>
                    </BarDropdownMenu>
                </BarDropdown>
            </BarItem>
            <BarItem>
                <BarDropdown>
                    <BarDropdownToggle>Dropdown</BarDropdownToggle>
                    <BarDropdownMenu>
                        <BarDropdownItem To="/eventcreator">No</BarDropdownItem>
                        <BarDropdownDivider />
                        <BarDropdownItem To="/fetchusers">No</BarDropdownItem>
                    </BarDropdownMenu>
                </BarDropdown>
            </BarItem>
        </BarStart>
        <BarEnd>
            <BarItem>
                <Button Clicked="@LogOut" Color="Color.Primary" Type="ButtonType.Link" To="/">Log out</Button>
            </BarItem>
        </BarEnd>
    </BarMenu>
</Bar>

@code {

    private void LogOut()
    {
        Login.user = new User();
        StateHasChanged();
    }
}